<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="ferp.dao.C2_Dao">

	<select id="r8101ProductSch" resultType="product" parameterType="product">
		SELECT * FROM Product
	</select>
	<select id="r8201ProductSch" resultType="product" parameterType="product">
		SELECT * FROM Product
		WHERE productNum LIKE '%'||#{productNum}||'%'
		AND category LIKE '%'||#{category}||'%'
		AND productName LIKE '%'||#{productName}||'%'
		AND opposite LIKE '%'||#{opposite}||'%'
		AND remark LIKE '%'||#{remark}||'%'
	</select>

</mapper>